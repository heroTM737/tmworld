$(document).ready(function () {
    var width = $(window).width();
    var height = $(window).height();
    console.log(width + "---" + height);

//    var r = 10;
//    for (var i = 0; i < 100; i++) {
//        for (var j = 0; j < 100; j++) {
//            var red = Math.floor(Math.random() * 1000) % 256;
//            var green = Math.floor(Math.random() * 1000) % 256;
//            var blue = Math.floor(Math.random() * 1000) % 256;
//            var alpha = Math.random();
//            var style = "background-color: rgba(" + red + "," + green + "," + blue + "," + alpha + "); top: " + j * r + "px; left: " + i * r + "px; width: " + r + "px; height: " + r + "px;";
//            $("#background").append("<div class='bgs' style='" + style + "'><div>");
//        }
//    }
});